{
  "dependencies": {},
  "scripts": {
    "start": "node tools/serve-static.js",
    "stop": "for /f \"tokens=5\" %a in ('netstat -ano ^| findstr :8080') do taskkill /PID %a /F",
    "build": "node tools/build-all-poems.js",
    "build:all": "npm run build && npm run start"
  }
}
